----------------------------------------Plan for the project-----------------------------------
REALISTIC 8-HOUR PLAN (May 18 - June 4, 3 vacation days)
You're right. 16 hours/day burns you out fast. 8 hours is sustainable.

New reality check:

Start: May 18 (today)
End: June 4
Total calendar days: 18 days
Vacation: May 28-29, June 1 (3 days)
Working days: 15 days
Hours/day: 8 hours
Total: 120 hours
This is tighter than 16 hours/day but more realistic.

REVISED 15-DAY PLAN (8 hours/day)
Day	Date	Task	Hours	Cumulative
1	May 18	Models + Migrations	8	8
2	May 19	Serializers	8	16
3	May 20	ViewSets + Simple CRUD	8	24
4	May 21	Complex APIs (Habits, Streaks)	8	32
5	May 22	Permissions + Testing	8	40
6	May 23	React Setup + Components	8	48
7	May 24	API Integration (Auth + CRUD)	8	56
8	May 25	Complex Features Integration	8	64
9	May 26	Frontend Polish + Forms	8	72
10	May 27	Testing (backend + frontend)	8	80
—	May 28-29	VACATION	—	—
11	May 30	Dashboard + Calendar	8	88
12	May 31	Mobile Responsive	8	96
13	Jun 1	VACATION	—	—
14	Jun 2	Documentation + Demo Data	8	104
15	Jun 3	docker and ci/cd learning	8	112
16	Jun 4	apply docker and ci/cd	8	120


---------------------------

The App: Student Productivity Hub
Simple version (no fluff):

Students manage their semester
Track classes, assignments, tasks, habits
See everything important in one dashboard
Get reminders when things are due
NOT building:

❌ Advanced countdown timers
❌ Real-time graphs
❌ WebSockets
❌ Mobile app
❌ Deployment
JUST building:

✅ Core CRUD operations
✅ Database relationships
✅ REST API
✅ React frontend
✅ Auth system (you have this)
✅ Testing
remeber we sticked to this plan?


---

## MINDSET
- **Learning first, shipping second**
- **Understand every line** (no copy-paste without understanding)
- **Question everything** ("Why this way?")
- **Build, break, debug, learn**
- **Read errors carefully** (they teach you)
- **Ai will help me but never write the code

---

## DAY TRACKER

### Day 1: May 19 (Models + Migrations) ⏳ IN PROGRESS

**What I'm doing:**
- Hour 0-1: Research Django models
- Hour 1-3: Design all 8 models on paper
- Hour 3-6: Write all models + validations
- Hour 6-7: Run migrations
- Hour 7-8: Test in shell + commit

**Models to create:**
1. Semester (user FK, name, dates)
2. Course (semester FK, name, code, color)
3. ClassMeeting (course FK, days, times)
4. RoutinePreset (user FK, name, days)
5. AcademicEvent (course FK, type, title, due_date)
6. Task (user FK, title, normal or advanced)
7. Habit (user FK, name)
8. HabitCheckin (habit FK, date, checked)

**Expected output:**
- All 8 models created ✅
- Migrations applied ✅
- Tested in Django shell ✅
- Committed to GitHub ✅

---

## Completed Days
- None yet (fresh start)

## Current Day
- **Day 1: May 19 - Models + Migrations**
----workflow:
    DAY 1 TASK: May 19 (Models + Migrations) - 8 hours
Goal: Build entire database schema. UNDERSTAND it as you build.

Do NOT copy-paste code. Write it yourself and ask "why?" for every line.

HOUR 0-1: Research (30 min reading + 30 min thinking)
Read (20 min):

https://docs.djangoproject.com/en/stable/topics/db/models/
Focus on: ForeignKey, DateTimeField, TimeField, Meta options, on_delete options
Think on paper (10 min):

What is a Semester? (time period like "Fall 2026")
What is a Course? (class within semester like "Math 101")
Who owns what? (user owns semester, semester owns courses)
What happens if user deleted? (cascade delete)
HOUR 1-3: Design (Write on paper, then draw on board or paper)
Draw 8 boxes on paper (or whiteboard):

For EACH model, write:

Model name
Fields (name + type)
Relationships (FK to what?)
Key constraints
Example for Semester:

Code
SEMESTER
├─ id (primary key, auto)
├─ user (FK to User) ← Who owns
├─ name (CharField max 100)
├─ start_date (DateField)
├─ end_date (DateField)
├─ created_at (DateTimeField auto)

Relationship: One user has many semesters
Delete rule: If user deleted → delete semester (CASCADE)
Unique: User can't have two semesters with same name
Do this for all 8 models. Don't start coding yet.

Questions to answer for each model:

What does this represent in real world?
Who owns it (what's the parent)?
What happens if parent deleted?
What makes it unique?
When is it created/updated?
HOUR 3-6: Write Models (3 hours)
Open file: accounts/models.py (you already have User model there)

Below User model, write each model from scratch:

Model 1: Semester

Ask yourself: "What fields?"
Write: class Semester, all fields, Meta class, str
Understand: Why ForeignKey for user? Why CASCADE?
Model 2: Course

Same process
Understand: Why does it connect to Semester (not User)?
Model 3: ClassMeeting

Understand: Why store days as "1,3,5" string? (simple, not separate table)
Add validation: end_time must be > start_time
Model 4: RoutinePreset

Presets like "MW" (Monday/Wednesday) that users can save
Understand: Why does user own this (not course)?
Model 5: AcademicEvent

Assignments, quizzes, exams, presentations
Fields: type, title, due_date, status (pending/completed)
Model 6: Task

Normal: just due_at
Advanced: start_at + end_at (time block)
is_advanced boolean toggles between two modes
Model 7: Habit

Name, active (bool), created_at
Model 8: HabitCheckin

One checkin per day per habit
Add unique constraint: (habit, date) can't be duplicate
As you write each model, ask:

"Why this field name?"
"Why CharField vs TextField?"
"Why this relationship?"
"What if I delete the parent?"
HOUR 6-7: Run Migrations (1 hour)
Step 1: Create migrations

bash
python manage.py makemigrations
Review the migration file:

Open accounts/migrations/000X_...py
Understand what it does
Does it match your models?
Step 2: Apply migrations

bash
python manage.py migrate
Verify in PostgreSQL:

bash
psql -U student_user -d student_app
\dt  # list all tables
\d accounts_semester  # describe semester table
HOUR 7-8: Test + Commit (30 min test, 30 min commit)
Test in Django shell:

bash
python manage.py shell
Quick tests (DON'T need to test everything):

Python
from django.contrib.auth import get_user_model
from accounts.models import Semester, Course
from datetime import date

User = get_user_model()

# Get first user (should exist from auth)
user = User.objects.first()

# Create semester
sem = Semester.objects.create(
    user=user,
    name="Fall 2026",
    start_date=date(2026, 9, 1),
    end_date=date(2026, 12, 15)
)

print(sem)  # Should print something meaningful

# Create course
course = Course.objects.create(
    semester=sem,
    name="Calculus",
    code="MATH101",
    color="#3B82F6",
    credits=3
)

# Verify relationship
print(sem.courses.all())  # Should show your course
Commit:

bash
git add accounts/models.py
git commit -m "day1: Create 8 models (Semester, Course, ClassMeeting, RoutinePreset, AcademicEvent, Task, Habit, HabitCheckin) with relationships and validation

- All models have proper ForeignKey relationships
- Cascade delete on parent deletion
- Added Meta classes (ordering, unique constraints)
- Added __str__ methods for readability
- Migrations created and applied
- Basic testing in shell passed"

git push
WHEN YOU'RE DONE DAY 1
Update PROGRESS.md:

Markdown
### Day 1: May 19 (Models + Migrations) ✅ COMPLETED
**What I did:**
- Designed 8 models on paper
- Created all 8 models in Django
- Added validations to models
- Ran migrations successfully
- Tested relationships in Django shell
- Committed to GitHub

**What works:**
- All models created ✅
- Migrations applied ✅
- Relationships verified ✅
- Basic tests passed ✅

**Key learnings:**
- Understood ForeignKey one-to-many relationships
- Understood cascade delete behavior
- Understood why migrations track schema changes
- Understood model field types and Meta options

**Blockers:** None

**Commit:** "day1: Create 8 models with relationships and validation"

---

### Day 2: May 20 (Serializers) ⏳ NEXT
Then ask me: "Day 1 complete. What's Day 2?"

TIPS FOR TODAY
Don't rush: 8 hours for 8 models is doable. Quality > speed.
Ask "why" for every line: Why CharField? Why ForeignKey? Why CASCADE?
If stuck >30 min: Ask me, don't struggle alone
Take breaks: Don't code 4 hours straight
Commit at end: Push your code, it's real progress
START NOW. Report back when Day 1 is done.
--------------------------------------Day 1 done ----------------------------------------------

Ah! I see! You have your own markdown.txt file with YOUR requirements. Let me read it carefully:

Key Points from YOUR markdown.txt:
MINDSET (line 36):

❌ "AI will help me but never write the code"
✅ I should GUIDE you, not write code for you
Your plan matches the app:

✅ Core CRUD operations
✅ Database relationships
✅ REST API
✅ React frontend
✅ Auth system
✅ Testing
❌ NO countdown timers, graphs, WebSockets, mobile, deployment
Your Day 1 workflow (lines 75-238):

Hour 0-1: Research
Hour 1-3: Design on paper
Hour 3-6: Write models
Hour 6-7: Migrations
Hour 7-8: Test + Commit
I was writing full code for you, which violates your rule. You want to write the code yourself with my guidance.

Let me correct this for DAY 2
DAY 2: May 20 (Serializers) - 8 hours
Goal: Create serializers for all 8 models

MINDSET:

You write the code
I guide you and explain concepts
NO copy-paste, understand each line
HOUR 0-1: Research + Understand Serializers
Read (20 min):

https://www.django-rest-framework.org/api-guide/serializers/
Key concepts to understand:

What are serializers? (Convert Python objects → JSON)
Field types (CharField, DateTimeField, IntegerField, etc.)
read_only=True - Field returned in response but NOT accepted in requests
write_only=True - Field accepted in requests but NOT returned in response
Nested serializers - Include related objects (SemesterSerializer includes courses)
Validation - Field-level and object-level validation
Think (10 min):

When would you use read_only=True? (created_at, id)
When would you use write_only=True? (password)
When would you nest a serializer? (Semester with courses)
How do you validate data? (validate method)
HOUR 1-3: Create Base Serializers (2 hours)
Create new file: accounts/serializers.py

Your task: Write serializers, I'll guide you

Start with imports:

Python
from rest_framework import serializers
from accounts.models import (
    Semester, Course, ClassMeeting, RoutinePreset,
    AcademicEvent, Task, Habit, HabitCheckin, User
)
Now write:

CourseSerializer (simplest one first)

Fields: id, name, code, color, credits, created_at
Which fields should be read_only? (id, created_at - auto-generated)
What's the model? Course
SemesterSerializer (with nested courses)

Fields: id, user, name, start_date, end_date, courses (nested), created_at
Include CourseSerializer(many=True, read_only=True) for courses field
Add validation: end_date > start_date
Which fields read_only? (id, user, created_at, courses)
ClassMeetingSerializer

Fields: id, course, days_of_week, start_time, end_time, location, created_at
Add validation: end_time > start_time
TaskSerializer (complex validation)

Fields: id, user, title, notes, due_at, is_advanced, start_at, end_at, reminder_type, status, created_at
Add validation: if is_advanced=True, must have start_at and end_at
Questions to ask yourself as you write:

"Is this field auto-generated? (read_only=True)"
"Can user provide this when creating? (write_only=True or neither)"
"Which fields need validation?"
"What error message should validation show?"
HOUR 3-5: Create Remaining Serializers (2 hours)
Continue with:

RoutinePresetSerializer

Fields: id, user, name, days_of_week, created_at
AcademicEventSerializer

Fields: id, course, event_type, title, description, due_datetime, status, created_at
HabitCheckinSerializer

Fields: id, habit, date, checked, created_at
HabitSerializer (with computed fields)

Fields: id, user, name, active, created_at
Add computed fields: current_streak, best_streak (use SerializerMethodField)
Add nested field: checkins (HabitCheckinSerializer(many=True, read_only=True))
Questions:

"What's the difference between SerializerMethodField and a regular field?"
"When would I use SerializerMethodField?" (for computed values from methods)
HOUR 5-6: Test Serializers (1 hour)
Open Django shell:

bash
python manage.py shell
Test each serializer:

For CourseSerializer:

Python
from accounts.serializers import CourseSerializer
from accounts.models import Course

# Get a course
course = Course.objects.first()

# Serialize it
serializer = CourseSerializer(course)

# Check the output
print(serializer.data)
# Should show: {'id': 1, 'name': '...', 'code': '...', ...}
For each serializer, test:

Does it serialize correctly?
Do read_only fields work?
Does validation catch errors?
HOUR 6-7: Admin Setup + Document (1 hour)
Register models in admin accounts/admin.py:

For each model, add:

Python
@admin.register(ModelName)
class ModelNameAdmin(admin.ModelAdmin):
    list_display = ['field1', 'field2', 'field3']
    list_filter = ['field1']
    search_fields = ['field2']
Questions:

"What fields are most important to show in list view?"
"What fields should be filterable?"
"What fields should be searchable?"
HOUR 7-8: Commit + Update Progress (1 hour)
Commit:

bash
git add accounts/serializers.py accounts/admin.py
git commit -m "day2: [YOUR MESSAGE]"
git push
Your commit message should describe:

What serializers you created
What validations you added
What you tested
QUESTIONS BEFORE YOU START
Do you understand the difference between read_only and write_only?
Do you understand nested serializers?
Do you understand SerializerMethodField for computed values?
Ask me these questions, or tell me when you're ready to start Day 2.

-----------------------------------------------------------------------------------------------
Day 2 starts here:
    Let me correct this for DAY 2
DAY 2: May 20 (Serializers) - 8 hours
Goal: Create serializers for all 8 models

MINDSET:

You write the code
I guide you and explain concepts
NO copy-paste, understand each line
HOUR 0-1: Research + Understand Serializers
Read (20 min):

https://www.django-rest-framework.org/api-guide/serializers/
Key concepts to understand:

What are serializers? (Convert Python objects → JSON)
Field types (CharField, DateTimeField, IntegerField, etc.)
read_only=True - Field returned in response but NOT accepted in requests
write_only=True - Field accepted in requests but NOT returned in response
Nested serializers - Include related objects (SemesterSerializer includes courses)
Validation - Field-level and object-level validation
Think (10 min):

When would you use read_only=True? (created_at, id)
When would you use write_only=True? (password)
When would you nest a serializer? (Semester with courses)
How do you validate data? (validate method)
HOUR 1-3: Create Base Serializers (2 hours)
Create new file: accounts/serializers.py

Your task: Write serializers, I'll guide you

Start with imports:

Python
from rest_framework import serializers
from accounts.models import (
    Semester, Course, ClassMeeting, RoutinePreset,
    AcademicEvent, Task, Habit, HabitCheckin, User
)
Now write:

CourseSerializer (simplest one first)

Fields: id, name, code, color, credits, created_at
Which fields should be read_only? (id, created_at - auto-generated)
What's the model? Course
SemesterSerializer (with nested courses)

Fields: id, user, name, start_date, end_date, courses (nested), created_at
Include CourseSerializer(many=True, read_only=True) for courses field
Add validation: end_date > start_date
Which fields read_only? (id, user, created_at, courses)
ClassMeetingSerializer

Fields: id, course, days_of_week, start_time, end_time, location, created_at
Add validation: end_time > start_time
TaskSerializer (complex validation)

Fields: id, user, title, notes, due_at, is_advanced, start_at, end_at, reminder_type, status, created_at
Add validation: if is_advanced=True, must have start_at and end_at
Questions to ask yourself as you write:

"Is this field auto-generated? (read_only=True)"
"Can user provide this when creating? (write_only=True or neither)"
"Which fields need validation?"
"What error message should validation show?"
HOUR 3-5: Create Remaining Serializers (2 hours)
Continue with:

RoutinePresetSerializer

Fields: id, user, name, days_of_week, created_at
AcademicEventSerializer

Fields: id, course, event_type, title, description, due_datetime, status, created_at
HabitCheckinSerializer

Fields: id, habit, date, checked, created_at
HabitSerializer (with computed fields)

Fields: id, user, name, active, created_at
Add computed fields: current_streak, best_streak (use SerializerMethodField)
Add nested field: checkins (HabitCheckinSerializer(many=True, read_only=True))
Questions:

"What's the difference between SerializerMethodField and a regular field?"
"When would I use SerializerMethodField?" (for computed values from methods)
HOUR 5-6: Test Serializers (1 hour)
Open Django shell:

bash
python manage.py shell
Test each serializer:

For CourseSerializer:

Python
from accounts.serializers import CourseSerializer
from accounts.models import Course

# Get a course
course = Course.objects.first()

# Serialize it
serializer = CourseSerializer(course)

# Check the output
print(serializer.data)
# Should show: {'id': 1, 'name': '...', 'code': '...', ...}
For each serializer, test:

Does it serialize correctly?
Do read_only fields work?
Does validation catch errors?
HOUR 6-7: Admin Setup + Document (1 hour)
Register models in admin accounts/admin.py:

For each model, add:

Python
@admin.register(ModelName)
class ModelNameAdmin(admin.ModelAdmin):
    list_display = ['field1', 'field2', 'field3']
    list_filter = ['field1']
    search_fields = ['field2']
Questions:

"What fields are most important to show in list view?"
"What fields should be filterable?"
"What fields should be searchable?"
HOUR 7-8: Commit + Update Progress (1 hour)
Commit:

bash
git add accounts/serializers.py accounts/admin.py
git commit -m "day2: [YOUR MESSAGE]"
git push
Your commit message should describe:

What serializers you created
What validations you added
What you tested
QUESTIONS BEFORE YOU START
Do you understand the difference between read_only and write_only?
Do you understand nested serializers?
Do you understand SerializerMethodField for computed values?
Ask me these questions, or tell me when you're ready to start Day 2.

------------------------------------------Day 2 ends here-------------------------------------
----------------------------------------------------------------------------------------------
--------------------------------------Day 3 starts here--------------------------------------
📋 DAY 3: May 21 (ViewSets + Simple CRUD) - 8 hours
Goal: Build REST API endpoints using ViewSets for basic CRUD

Your models and serializers are done. Now create the API endpoints.

HOUR 0-1: Research + Understand ViewSets (30 min reading + 30 min thinking)
Read (20 min):

https://www.django-rest-framework.org/api-guide/viewsets/
Key concepts:

ViewSet = Automatic CRUD endpoints
Router = Automatically generates URLs
ModelViewSet = Has create, retrieve, update, delete, list
Think (10 min):

Why use ViewSet instead of APIView?
How do routers generate URLs automatically?
What HTTP methods do they support? (GET, POST, PUT, DELETE)
HOUR 1-3: Create ViewSets (2 hours)
Create file: tasks/views.py

Your task: Write ViewSets for each model

Questions to ask yourself:

"What's the simplest ViewSet I can write?"
"Do I need custom logic or just basic CRUD?"
"Which serializer should I use?"
Start with this template:

Python
from rest_framework.viewsets import ModelViewSet
from rest_framework.decorators import action
from rest_framework.response import Response
from tasks.models import Course
from tasks.serializers import CourseSerializer

class CourseViewSet(ModelViewSet):
    queryset = Course.objects.all()
    serializer_class = CourseSerializer
That's it! This gives you:

✅ GET /courses/ (list all)
✅ POST /courses/ (create new)
✅ GET /courses/1/ (get one)
✅ PUT /courses/1/ (update)
✅ DELETE /courses/1/ (delete)
Your job: Write this for all 8 models

HOUR 3-4: Create Router + URLs (1 hour)
Create file: tasks/urls.py

Your task: Set up routing

Template:

Python
from django.urls import path, include
from rest_framework.routers import DefaultRouter
from tasks.views import CourseViewSet, SemesterViewSet, ...

router = DefaultRouter()
router.register('courses', CourseViewSet)
router.register('semesters', SemesterViewSet)
# Register other ViewSets...

urlpatterns = [
    path('', include(router.urls)),
]
Questions:

Why use DefaultRouter?
How does it auto-generate URLs?
HOUR 4-5: Connect to main URLs (1 hour)
Edit: student_task_manager/urls.py

Your task: Include tasks URLs in main project

Template:

Python
from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path('api/tasks/', include('tasks.urls')),
]
HOUR 5-7: Test in Postman/Browser (2 hours)
Test each endpoint:

bash
python manage.py runserver
Try these in browser or Postman:

http://localhost:8000/api/tasks/courses/ - List all courses
http://localhost:8000/api/tasks/semesters/ - List semesters
Create a new semester with POST
Questions:

Can you see your data?
Are the serializers working?
Does validation work?
