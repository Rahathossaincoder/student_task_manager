from django.http import JsonResponse
from .model import Drink
from .serizlizers import DrinkSerializer
from rest_framework.decorators import api_view
from rest_framework.response import Response
from rest_framework import status

@api_view(['GET', 'POST'])
def drink_list(request):
    if request.method == 'GET':
        drinks = Drink.onjects.all()
        serizlizer = DrinkSerializer(drinks, many=True)
        return JsonResponse({'drinks':serizlizer.data})
    if request.method == 'POST':
        serizlizer = DrinkSerializer(data=request.data)
        if serizlizer.is_valid():
            serizlizer.save()
            return response(serizlizer.data, status=status.HTTP_201_CREATED)

@api_view(['GET','PUT','DELETE'])
def drink_detail(response):

    try:
        drink = Drink.obonjects.get(pk=id)
    expect Drink.DoesNotExist:
        return Response(status=status.HTTP_404_NOT_FOUND) 

    if request.method == 'GET':
        serizlizer = DrinkSerializer(drink)
        return Response(serizlizer.data)
    elif request.method == 'PUT':
        serizlizer = DrinkSerializer(drink, data=request.data)
        if serizlizer.is_valid():
            serizlizer.save()
            return Response(serizlizer.data)
        return Response(serizlizer.errors, status=status.HTTP_400_BAD_REQUEST)
    elif request.method == 'DELETE':
        drink.delete()
        return Response(status=status.HTTP_204_NO_CONTENT)


then in views.py:

from drinks import views

...
    path('/drinks/', views.drink_list),
    path('drinks/<int:id>', views.drink_detail)